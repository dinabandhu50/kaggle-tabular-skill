# Phase 5 search space for LightGBM. Loaded by src/optimize/optuna_search.py::param_space_from_config.
# Changing the search is a config edit here, not a code change.
learning_rate: {type: float, low: 0.01, high: 0.1, log: true}
num_leaves: {type: int, low: 15, high: 255}
min_child_samples: {type: int, low: 5, high: 100}
feature_fraction: {type: float, low: 0.6, high: 1.0}
bagging_fraction: {type: float, low: 0.6, high: 1.0}
reg_lambda: {type: float, low: 0.001, high: 10.0, log: true}
